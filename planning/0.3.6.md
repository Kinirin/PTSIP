# PTSIP Tool 0.3.6 — Primary Lifecycle Ownership Foundation and Release Plan

> **Status:** IN DEVELOPMENT — WU-00 through WU-06 complete; WU-07 ACTIVE  
> **Development branch:** `tool-0.3.6-lifecycle-ownership`  
> **Target Tool version:** `0.3.6`  
> **Published baseline:** Tool `0.3.5`  
> **Specification family:** `0.3.6-draft`  
> **Current bound Specification snapshot:** `d6995ed232e845b88d8235b851e80ab54b7804ea`  
> **WU-04H completion snapshot:** `9fac22d31333346dbe56a12dee890df1229d560b`  
> **WU-04I entry baseline:** `713dee236def879e00dadca894add98d65ffb754`  
> **WU-04 exact-SHA completion snapshot:** `eadb07f78f9690ca0180bfbba194c5c9602e1838`  
> **WU-04 verification run/job:** `32588090216 / 97067421324`  
> **WU-05 exact entry baseline:** `c87e597409592f0794a5d404d621b6f73dd00cc6`  
> **WU-05 exact dogfood verification snapshot:** `5010fa81ebd104eb329f29217d4c5fecec51cacb`  
> **WU-06 exact entry baseline:** `bdd77ef3f026a83bb0fd0099144aebea1de55365`  
> **WU-06 entry-document commit:** `59224b9df25db34f4af58e48eaeac0b1db8bf187`  
> **WU-06 exact verification snapshot:** `a94c50130a694dba937708403520417719aec1e1`  
> **WU-06 verification run/job:** `32598727026 / 97093629017`  
> **WU-07 exact entry baseline:** `8b2c0819e10b58902a780a094a0f52c603c39fba`  
> **WU-07 entry-document commit:** `0e438e7544ebbfee1f1692086d31d9d6f539d297`  
> **WU-07 selected strategy:** `B — Release Contract Strengthening`  
> **Planning split baseline:** `3529a32c862e1d43a91f732ced36358b4e13e1d9`

## 1. Purpose

This document is the release-level work-unit index and entry-gate authority for Tool `0.3.6`.

Tool `0.3.6` has a deliberately bounded release goal: complete and release the **Primary Lifecycle Ownership foundation** implemented around Responsibility Map v2 and the Effective Responsibility Map.

The evidence-driven Tool `0.3.5` migration system that was originally planned as WU-05 through WU-10 is no longer part of the Tool `0.3.6` completion gate. That work continues as Tool `0.3.6.1` under [`planning/0.3.6.1.md`](0.3.6.1.md).

Detailed Tool `0.3.6` implementation records remain under:

```text
planning/0.3.6/
```

## 2. Canonical Tool 0.3.6 architecture boundary

Canonical lifecycle classifications are exactly:

```text
PRODUCT
DEVELOPMENT_TOOLING
DELIVERY
OPERATIONS
NEUTRAL_CONTRACT
```

`TOOLCHAIN` is a Tool `0.3.5` legacy classification only. It is not a canonical Tool `0.3.6` classification or alias.

These axes remain independent:

```text
classification
    = primary lifecycle ownership

roles
    = coarse responsibility characteristics

relationships
    = project-owned typed directed semantics

source_mode / derived origin
    = declaration/materialization provenance

VPMS Verification Purpose
    = what verification protects/verifies
```

Canonical Responsibility Map source modes remain:

```text
explicit
template
hybrid
```

Template selection is explicit and immutable-revision-bound. Materialization is deterministic and non-authoritative. Repository evidence, template materialization, and discovery MUST NOT become architecture authority.

## 3. Effective Responsibility Map boundary

All source declaration modes resolve to one downstream semantic model:

```text
explicit ----+
template ----+--> Canonical Effective Responsibility Map
hybrid ------+
                    |
                    +--> components
                    +--> associated_artifacts
                    +--> relationships
                    +--> component_dependency_policy
                    +--> policies
```

The source declaration and exact template identity remain separately available. Downstream consumers use validated resolved architecture rather than independently reinterpreting raw source YAML.

## 4. WU-04 implementation status

| Stage | Status | Responsibility |
| --- | --- | --- |
| `WU-04A` | **COMPLETE** | template catalog identity |
| `WU-04B` | **COMPLETE** | deterministic materializer core |
| `WU-04C` | **COMPLETE** | declaration authority + `source_mode` + effective-map boundary |
| `WU-04D` | **COMPLETE** | `ResolvedProfile` + digest + derived provenance |
| `WU-04E` | **COMPLETE — EXACT-SHA VERIFIED** | profile validation consumes effective map |
| `WU-04F` | **COMPLETE — EXACT-SHA VERIFIED** | conformance consumes effective map |
| `WU-04G` | **COMPLETE — EXACT-SHA VERIFIED** | clarification/adoption integration, answer v2, hybrid safe apply, exact profile-path control plane |
| `WU-04H` | **COMPLETE — VERIFIED** | VPMS narrow read-only effective-map integration |
| `WU-04I` | **COMPLETE — EXACT-SHA VERIFIED** | final regression + WU-04 completion |

Key records:

```text
planning/0.3.6/WU-04C-declaration-authority-effective-map.md
planning/0.3.6/WU-04D-resolved-profile-digest-provenance.md
planning/0.3.6/WU-04E-validation-effective-map.md
planning/0.3.6/WU-04F-conformance-effective-map.md
planning/0.3.6/WU-04G-clarification-adoption-effective-map.md
planning/0.3.6/WU-04G-G5-verification-closure.md
planning/0.3.6/WU-04H-vpms-effective-map-integration.md
planning/0.3.6/WU-04I-final-regression-wu04-completion.md
planning/0.3.6/WU-05-repository-dogfood-self-evaluation.md
planning/0.3.6/WU-06-full-regression-package-distribution-verification.md
planning/0.3.6/WU-07-final-specification-freeze-release-preparation.md
```

WU-04H completed at:

```text
9fac22d31333346dbe56a12dee890df1229d560b
```

with repository-local verification:

```text
H focused verification                          35 passed / exit=0
VPMS integration + architecture isolation       41 passed / exit=0
repository self-profile                         4 passed / exit=0
```

WU-04I then remediated all eight historical full-regression failures without changing production architecture semantics and completed exact-SHA verification at:

```text
eadb07f78f9690ca0180bfbba194c5c9602e1838
```

Final WU-04I evidence:

```text
GitHub Actions run/job:       32588090216 / 97067421324
runner:                       self-hosted Windows X64
Python:                       3.14.6
complete repository pytest:   327 passed / 0 failed
repository self-profile:      valid=true, warnings=[], unassigned_count=0
clarification snapshot:       STABLE / no clarification required
local gate:                   NO_DECISION_REQUIRED
sdist + wheel build:          PASS
twine check:                  PASS
built-wheel reinstall smoke:  PASS
self-hosted/tooling-test:      success
```

Therefore WU-04 is COMPLETE. The post-verification planning/status commits are closure records; the exact implementation verification authority remains `eadb07f...`.

## 5. Overall Tool 0.3.6 work units

| Work Unit | Status | Responsibility |
| --- | --- | --- |
| `WU-00` | **COMPLETE** | `0.3.6-draft` normative baseline |
| `WU-01` | **COMPLETE** | lifecycle ontology and boundary rules |
| `WU-02` | **COMPLETE** | roles, typed relationships, associated artifacts |
| `WU-03` | **COMPLETE** | Responsibility Map v2 canonical schema/runtime activation |
| `WU-04` | **COMPLETE — EXACT-SHA VERIFIED** | template catalog + deterministic materialization + effective-map consumption |
| `WU-05` | **COMPLETE — DOGFOOD REVIEWED** | repository dogfood / self-evaluation of the Tool `0.3.6` foundation |
| `WU-06` | **COMPLETE — EXACT-SHA VERIFIED** | full regression + package/distribution verification |
| `WU-07` | **ACTIVE — Strategy B** | final Specification freeze + release-contract strengthening + release preparation |

The earlier roadmap numbering is intentionally retired for the Tool `0.3.6` canonical plan:

```text
former WU-11 -> current WU-05
former WU-12 -> current WU-06
former WU-13 -> current WU-07
```

Historical commits may still mention the former numbers. Current planning and future execution use only the work-unit numbering in this document.

## 6. Tool 0.3.6 release sequence

```text
WU-04 COMPLETE
        |
        v
WU-05 COMPLETE
    repository dogfood / self-evaluation
        |
        v
WU-06 COMPLETE / EXACT-SHA VERIFIED
    full regression / package / distribution verification
        |
        v
WU-07 ACTIVE — Strategy B
    final Specification freeze / release-contract strengthening / release preparation
        |
        v
Tool 0.3.6 release
```

WU-05 dogfood was limited to the architecture foundation actually shipped by Tool `0.3.6`. Automatic or assisted Tool `0.3.5 -> 0.3.6` migration remains outside the Tool `0.3.6` release prerequisite.

WU-05 was entered after a fresh branch-HEAD read at exact baseline:

```text
c87e597409592f0794a5d404d621b6f73dd00cc6
```

Repository-consumer dogfood was reviewed against exact snapshot:

```text
5010fa81ebd104eb329f29217d4c5fecec51cacb
```

Completion evidence:

```text
validate:                          PASS
source_mode:                       explicit
materialized:                      true
Effective Map unassigned_count:    0
Effective Map digest:              sha256:c009d4fb98daa8a3dc0fd2dfbdd974b467cf107c8c1e7814e460059f4c0c4a01
conform:                           ENFORCED / INCOMPLETE / reviewed
definite architecture violations:  0
conformance audit:                 PASS
conformance snapshot:              STABLE
clarify:                           NO_CLARIFICATION_REQUIRED / STABLE
local gate:                        NO_DECISION_REQUIRED / LOCAL / decisions=[]
post-gate working tree:             CLEAN
confirmed WU-05 A-C defects:       NONE
Specification defect:              NONE
profile/source correction:          NONE
```

The conformance `INCOMPLETE` result was retained rather than forced green. The observed dependency-name and independent-build manifest limitations remained fail-closed evidence gaps rather than false architecture violations. Product Artifact / exact artifact binding evidence was handed to WU-06 for release closure.

The execution/closure authority is `planning/0.3.6/WU-05-repository-dogfood-self-evaluation.md`.

WU-06 was entered only after WU-05 closure records were committed and a fresh development-branch HEAD read returned:

```text
bdd77ef3f026a83bb0fd0099144aebea1de55365
```

That SHA is the exact WU-06 entry baseline. The detailed WU-06 stage document was then created at descendant commit:

```text
59224b9df25db34f4af58e48eaeac0b1db8bf187
```

WU-06 completed technical verification against exact candidate:

```text
a94c50130a694dba937708403520417719aec1e1
```

with release-candidate evidence:

```text
GitHub Actions run/job:             32598727026 / 97093629017
runner capability:                  self-hosted Windows X64
Python:                             3.14.6
complete repository pytest:         327 passed / 0 failed
Tool/package version:               0.3.6
Specification:                      0.3.6-draft @ d6995ed232e845b88d8235b851e80ab54b7804ea
repository self-profile:            valid=true, warnings=[], unassigned_count=0
sdist + wheel build:                PASS
twine check:                        PASS
Product Artifact evidence:          PASS
artifact exact-snapshot binding:    PASS
artifact PTSIP-PKG-001 violations:  0
artifact-aware conform:             INCOMPLETE / reviewed
built-wheel reinstall smoke:        PASS
VPMS compatibility smoke:           PASS
self-hosted/tooling-test:           success
```

The remaining `INCOMPLETE` result does not represent a Product Artifact failure; artifact-evidence blocking gaps were cleared and no definite `PTSIP-PKG-001` violation remained. Previously reviewed dependency-name and independent-build evidence limitations remain fail-closed evidence sufficiency observations rather than false architecture violations.

WU-06 corrections were limited to the existing `tooling-test.yml` release-verification path and its release-readiness regression contract. Post-verification planning/status commits are closure records and do not replace `a94c501...` as the exact verification authority.

Its execution/closure authority is `planning/0.3.6/WU-06-full-regression-package-distribution-verification.md`.

WU-07 was entered only after WU-06 closure propagation completed and a fresh development-branch HEAD read returned:

```text
8b2c0819e10b58902a780a094a0f52c603c39fba
```

That SHA is the exact WU-07 entry baseline. The detailed WU-07 stage document was created at descendant commit:

```text
0e438e7544ebbfee1f1692086d31d9d6f539d297
```

The maintainer selected **Strategy B — Release Contract Strengthening**. WU-07 owns the final Specification audit, exact bound-content release guard, Tool release-note closure, operational current-state cleanup, publication Product Artifact verification, release-readiness regression contracts, final exact-SHA development verification, and later exact-main release handoff. Strategy C Build Once / attestation-oriented release-pipeline redesign remains out of scope.

The WU-07 entry audit found no genuine normative Specification defect in the already-bound `0.3.6-draft @ d6995ed...` asset set. No normative revision movement is authorized merely by release-contract, workflow, test, release-note, planning, or status corrections.

## 7. Migration continuation boundary

The following chain has moved out of Tool `0.3.6`:

```text
Evidence model / candidate evidence expansion
    -> stronger discovery and provenance normalization
    -> Tool 0.3.5 legacy reader
    -> migration analyzer
    -> component split / relationship / associated-artifact proposals
    -> migration preview and owner confirmation
    -> safe migration apply
```

Its canonical continuation is:

```text
planning/0.3.6.1.md
planning/0.3.6.1/WU-01-candidate-discovery-evidence-expansion.md
planning/0.3.6.1/WU-02-evidence-provenance-normalization.md
planning/0.3.6.1/WU-03-tool-035-legacy-reader.md
planning/0.3.6.1/WU-04-lifecycle-migration-analyzer.md
planning/0.3.6.1/WU-05-target-proposals.md
```

The final preview/confirmation/safe-apply stage is `0.3.6.1` WU-06 and remains master-plan-only until its actual entry unless the maintainer explicitly pre-creates its detailed stage document.

## 8. VPMS boundary

PTSIP lifecycle classification and VPMS Verification Purpose remain independent.

Current VPMS purpose vocabulary may retain its own compatibility terminology; VPMS `TOOLCHAIN` is not a canonical PTSIP Tool `0.3.6` classification.

PTSIP core MUST NOT depend on VPMS. VPMS consumes only a narrow read-only projection of already-resolved effective PTSIP metadata and does not implement template/hybrid semantics or migration authority.

## 9. Specification binding contract

For Tool `X.Y.Z`:

```text
Tool X.Y.Z
    -> Specification X.Y.Z-draft
    -> immutable SPEC_REVISION
```

Current Tool `0.3.6` binding:

```text
Specification: 0.3.6-draft
SPEC_REVISION: d6995ed232e845b88d8235b851e80ab54b7804ea
```

A new immutable Specification revision is required only for a genuine normative change. Regression closure, test migration, dogfood execution, release-contract hardening, or documentation-only release preparation must not move `SPEC_REVISION` by themselves.

WU-04I, WU-05, and WU-06 did not establish a normative revision defect. WU-07 final audit has so far likewise established no normative defect requiring revision movement. The current release contract therefore retains `d6995ed...` and strengthens detection of release-source drift from that exact snapshot.

## 10. Self-hosted verification and release resource policy

Repository regression, release preparation, and distribution build default to eligible self-hosted Windows X64 execution with PowerShell and Python 3.14 available through `py -3.14`.

WU-04 closure is backed by successful `self-hosted/tooling-test` status on exact SHA `eadb07f78f9690ca0180bfbba194c5c9602e1838`.

WU-06 closure is backed by successful `self-hosted/tooling-test` status on exact SHA `a94c50130a694dba937708403520417719aec1e1`, run/job `32598727026 / 97093629017`. Earlier stage successes remain predecessor evidence only.

WU-07 requires its own successful exact-SHA self-hosted verification after all release-contract and documentation changes are complete. WU-06 success cannot be reused as final WU-07 candidate authority.

The final release path must preserve exact-SHA checkout verification. A stage-scoped pass does not convert a different final `main` SHA into release readiness.

The narrow GNU/Linux PyPI Trusted Publishing step may remain the GitHub-hosted exception where required by the publication boundary.

## 11. Entry discipline

Future Tool `0.3.6` work units are entered only after predecessor completion review and a fresh branch-HEAD read.

Required order:

```text
current stage complete
    -> fresh branch HEAD
    -> record predecessor completion
    -> create/update next stage document with exact entry SHA
    -> mark only that stage ACTIVE
```

WU-05 satisfied its entry and completion sequence. Its exact dogfood verification authority is `5010fa81ebd104eb329f29217d4c5fecec51cacb`.

WU-06 satisfied its entry and verification sequence:

```text
WU-05 closure complete
    -> fresh branch HEAD = bdd77ef3f026a83bb0fd0099144aebea1de55365
    -> detailed WU-06 stage document created
    -> WU-06 ACTIVE
    -> verification corrections classified and applied
    -> exact verification candidate = a94c50130a694dba937708403520417719aec1e1
    -> self-hosted/tooling-test = success
    -> WU-06 COMPLETE / EXACT-SHA VERIFIED
```

The WU-06 exact entry baseline remains `bdd77ef3f026a83bb0fd0099144aebea1de55365`; closure records do not replace exact verification authority `a94c50130a694dba937708403520417719aec1e1`.

WU-07 has now satisfied its entry sequence:

```text
WU-06 closure complete
    -> fresh branch HEAD = 8b2c0819e10b58902a780a094a0f52c603c39fba
    -> maintainer selects Strategy B — Release Contract Strengthening
    -> detailed WU-07 stage document created at 0e438e7544ebbfee1f1692086d31d9d6f539d297
    -> WU-07 ACTIVE
```

The WU-07 exact entry baseline remains `8b2c0819e10b58902a780a094a0f52c603c39fba`. Descendant implementation, documentation, planning, status, and eventual verification commits do not replace that entry authority.

Migration continuation documents under `planning/0.3.6.1/` are roadmap records only and do not authorize Tool `0.3.6.1` implementation while Tool `0.3.6` remains unreleased.

## 12. Completion boundary

Tool `0.3.6` development is complete when all of the following succeed:

- five-classification Primary Lifecycle Ownership semantics;
- Responsibility Map v2 canonical schema/runtime behavior;
- deterministic explicit/template/hybrid materialization;
- effective-map consumption across validation, conformance, clarification/adoption, and the narrow VPMS integration boundary;
- repository dogfood/self-evaluation for those Tool `0.3.6` capabilities;
- canonical/embedded contract coherence;
- required full regression, package, and distribution verification;
- final Specification freeze and release preparation.

WU-04 satisfies the architecture-foundation/effective-map portion of this completion boundary. WU-05 repository dogfood is COMPLETE at exact verification snapshot `5010fa81...`; WU-06 is COMPLETE / EXACT-SHA VERIFIED at exact verification snapshot `a94c501...`; WU-07 is ACTIVE from exact entry baseline `8b2c0819...` under Strategy B.

**Evidence-backed Tool `0.3.5` migration is no longer a Tool `0.3.6` completion requirement.** It is owned by Tool `0.3.6.1`.

Release readiness additionally requires the exact final development candidate to pass WU-07 exact-SHA verification and the exact final `main` SHA to satisfy Specification binding and the exact-SHA self-hosted release gate.