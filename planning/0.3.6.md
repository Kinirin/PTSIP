# PTSIP Tool 0.3.6 — Responsibility Map Generalization, Evidence-Driven Candidate Discovery, and Specification Discipline

> **Status:** PLANNED  
> **Target Tool version:** `0.3.6`  
> **Published baseline:** Tool `0.3.5`  
> **Required Specification family before release:** `0.3.6-draft`  
> **Primary goal:** generalize the repository-owned Responsibility Map without forcing migration, strengthen component candidate discovery with structural/runtime evidence, make VPMS visible in the normative Specification, and make Specification binding a mandatory release contract.

## 1. Scope consolidation

Tool `0.3.6` absorbs the previously separate `0.4.0` candidate-discovery plan because implementation has not yet begun and the two changes are directly related.

The former `0.4.0` plan focused on reducing dependency on repository-name and path heuristics when discovering component candidates. That work now becomes part of Tool `0.3.6` so Responsibility Map v2 and migration are built on stronger evidence from the beginning rather than on a heuristic layer that would immediately need redesign.

This consolidation does **not** authorize automatic Responsibility Map or template selection. Candidate discovery may become richer, but project-owned responsibility decisions remain explicit.

## 2. 0.3.5 premise carried forward

Tool `0.3.5` intentionally kept the Responsibility Map light. Its root `ptsip.yaml` demonstrates that a repository can declare real Product, Toolchain, and Neutral Contract ownership using the existing component declaration model without first introducing a new Responsibility Map schema.

Tool `0.3.6` builds on that evidence. It does **not** invalidate the current explicit declaration style. Instead, it adds optional standardization and migration paths for repositories that want them.

```text
existing explicit Responsibility Map
    remains valid

optional 0.3.6 migration
    may adopt a supported template
    or a template + project overrides
    or materialize a migrated explicit map
```

No repository is required to replace a working explicit map merely because templates exist.

## 3. Responsibility Map modes

Tool `0.3.6` must support three project-owned modes.

### Explicit

The repository declares the complete Responsibility Map directly, as Tool `0.3.5` does today. This remains the compatibility baseline and is not treated as legacy behavior.

### Template

The repository explicitly selects one supported, versioned Responsibility Map template. A template is never selected by directory-name heuristics or by candidate-discovery evidence alone.

### Hybrid

The repository selects a supported template and supplies project-owned overrides/extensions. Explicit repository declarations take precedence over template defaults inside the schema-defined override boundary. Ambiguous or conflicting overrides fail closed rather than being guessed.

## 4. Initial supported templates

The first catalog should remain small and useful. Tool `0.3.6` should include at least these common responsibility shapes, with exact stable IDs frozen during implementation:

1. Python package/library;
2. Python CLI/application;
3. mixed Product + Toolchain repository.

Templates standardize responsibility semantics and useful starting declarations. They do **not** make directory names normative, and they do not prevent a repository from using a completely custom explicit map.

Template identities must be versioned or revision-bound so a later PTSIP release cannot silently change the meaning of a previously selected template.

## 5. Evidence-driven candidate discovery

The previous candidate-discovery design relies partly on names and paths to identify likely repository components. Tool `0.3.6` must broaden candidate discovery so it can use repository evidence that is closer to actual execution, distribution, and dependency behavior.

The discovery engine should consider at least the following evidence classes where available:

- **invocation evidence** — commands, entry points, workflow invocations, executable scripts, launch configuration, task definitions, and other evidence that a repository artifact is actually invoked;
- **manifest evidence** — package/build/project manifests and declared package or application metadata;
- **dependency evidence** — imports, package dependencies, internal references, build dependencies, runtime/development dependency edges, and other dependency-graph evidence;
- **packaging evidence** — files or modules included in or excluded from built/distributed artifacts;
- **workflow/build evidence** — CI, build, release, test, and maintenance automation that demonstrates lifecycle responsibility;
- **explicit project declarations** — current `ptsip.yaml`, template selection, overrides, or other authoritative repository-owned metadata.

Path and name heuristics may remain as low-confidence discovery evidence where useful, but they must not dominate stronger evidence and must not become responsibility authority.

Candidate discovery produces **evidence**, not final ownership or purpose decisions.

```text
repository evidence
    -> candidate discovery
    -> candidate + evidence set
    -> project declaration / clarification / migration proposal
```

The discovery layer must preserve provenance so an agent or maintainer can see *why* a component candidate was proposed.

## 6. Candidate discovery and Responsibility Map boundary

Improved candidate discovery is intentionally separated from Responsibility Map authority.

The following are prohibited:

```text
"pyproject.toml exists"
    -> automatically choose Python package template

"tests/ exists"
    -> automatically classify repository as TOOLCHAIN

"src/ exists"
    -> automatically declare PRODUCT
```

Instead, evidence may support a proposal:

```text
manifest + packaging + invocation evidence
    -> strong Python-package candidate evidence
    -> suggest a compatible template or responsibility mapping
    -> maintainer/project declaration decides whether to adopt it
```

The same rule applies to coding agents: stronger discovery may reduce ambiguity, but it does not grant an agent authority to silently choose a template or rewrite project-owned responsibility facts.

## 7. Migration behavior

Migration is opt-in and preview-first:

```text
inspect existing declarations
    +
inspect candidate evidence
    -> propose template/mapping
    -> show semantic and textual diff
    -> maintainer chooses whether to apply
```

Migration must never silently rewrite a repository-owned Responsibility Map.

Two migration outputs should be supported:

- **template-backed migration** — keep a selected template identity and store only required project overrides/extensions;
- **materialized migration** — expand the selected template into a complete explicit project-owned map so runtime template resolution is no longer required.

Migration must preserve project-specific facts such as `purpose`, selectors, lifecycle ownership, shipping/runtime facts, consumers, analysis inputs, release ownership, and compatibility ownership unless the maintainer explicitly changes them.

If the selected template/override model cannot represent the current declaration losslessly, migration must stop and report the unresolved facts instead of dropping them.

Candidate-discovery evidence may help identify potential gaps or mismatches, but it must not override explicit project-owned facts during migration.

## 8. Resolution precedence

Responsibility resolution must be deterministic:

```text
explicit project override
    >
selected template declaration
    >
template default
```

A fully explicit map does not consult template defaults. Template selection must be explicit; PTSIP must not infer it from repository layout, package manager, language, framework, filenames, or candidate-discovery score.

`purpose` remains repository-owned descriptive intent. Tool `0.3.6` standardizes responsibility semantics and migration mechanics without forcing every project to use the same component IDs or purpose strings.

## 9. VPMS becomes Specification-visible

Tool `0.3.5` shipped VPMS while remaining bound to `0.3.4-draft`. Tool `0.3.6` must close the resulting agent-policy gap by creating a new `0.3.6-draft` Specification that normatively records at least:

- VPMS — Verification Purpose Management System;
- PTSIP classification and VPMS verification purpose as separate axes;
- `PRODUCT | TOOLCHAIN` verification purposes;
- Verification Case as the purpose-bound execution unit;
- Formula, Variables, Policy, Target, and Runner separation;
- Formula reuse without purpose collapse;
- PTSIP core MUST NOT depend on VPMS;
- VPMS may consume stable PTSIP metadata through a narrow read-only boundary;
- VPMS PASS does not imply PTSIP CONFORMANT, and PTSIP CONFORMANT does not imply functional verification PASS;
- Responsibility Map template/migration semantics introduced by Tool `0.3.6`;
- candidate discovery as evidence acquisition rather than responsibility authority;
- evidence provenance and no-path-only-inference requirements.

## 10. Mandatory Specification release contract

Starting with Tool `0.3.6`, release preparation must fail unless all of the following are true:

1. `pyproject.toml` declares the requested Tool version;
2. `src/ptsip/constants.py` binds `SPEC_VERSION` to `<Tool version>-draft`;
3. `SPEC_REVISION` is a full immutable Git commit SHA;
4. the Specification revision exists and is an ancestor of the release source;
5. canonical Specification files exist at that revision;
6. root `ptsip.yaml` binds the same Specification version, source, and revision;
7. `releasenote/spec-<SPEC_VERSION>.md` exists and is non-empty;
8. the exact release-candidate source SHA has passed the required self-hosted verification.

This is a release gate, not a documentation reminder. The pipeline must reject a release that omits the Specification work.

## 11. `AGENTS.md` and `MEMORY.md`

Root `AGENTS.md` and `MEMORY.md` are repository-operational inputs for coding agents.

- `AGENTS.md` defines required working behavior.
- `MEMORY.md` records durable current repository/release/architecture context.
- Neither replaces the normative Specification.
- Agents must read both before planning or modifying the repository.
- Agents must still read `ptsip.yaml` and the bound Specification.
- Agents must treat candidate-discovery output as evidence, not as permission to infer template choice or responsibility facts without project authority.

## 12. Self-hosted verification policy

GitHub-hosted Actions usage is constrained. Full repository testing and release-candidate verification use the single `.github/workflows/tooling-test.yml` workflow in manual self-hosted mode.

Expected runner identity:

```text
DESKTOP-5HCCQIR
```

The self-hosted test workflow must:

- use a self-hosted Windows runner;
- verify `RUNNER_NAME == DESKTOP-5HCCQIR` before repository work;
- require explicit host-ready acknowledgement;
- never run automatically on push, pull request, tag, schedule, or release;
- run the complete pytest/CLI/build/wheel smoke boundary when manually dispatched;
- support release-candidate verification for one exact SHA.

Before any coding agent or maintainer automation dispatches the self-hosted workflow, the user must be told that the runner will be used and must explicitly confirm that the host and PowerShell environment are ready.

The release-preparation workflow may remain GitHub-hosted for lightweight release orchestration, and the PyPI publication workflow may remain GitHub-hosted for Trusted Publishing. They must not duplicate the expensive full test suite once the self-hosted gate is active.

## 13. Non-goals

Tool `0.3.6` does not aim to:

- make a supported template mandatory;
- discard or auto-convert a valid Tool `0.3.5` explicit map;
- infer Responsibility Map mode from paths or repository naming;
- silently overwrite project-owned responsibility facts;
- make template component IDs mandatory for custom repositories;
- classify responsibility directly from candidate-discovery confidence;
- automatically select a template from manifest, dependency, packaging, invocation, or path evidence;
- remove all heuristic discovery when heuristic evidence remains useful as a low-confidence signal;
- collapse PTSIP classification and VPMS verification purpose;
- turn `MEMORY.md` or `AGENTS.md` into normative Specification documents;
- automatically power on or dispatch the self-hosted runner.

## 14. Work units

| Work Unit | Responsibility | Completion gate |
| --- | --- | --- |
| `WU-00` | Create and activate `0.3.6-draft` Specification baseline including VPMS, Responsibility Map v2, and discovery-evidence boundaries | Tool `0.3.6` cannot reach release preparation without an immutable Specification revision |
| `WU-01` | Define Responsibility Map v2 mode/identity/override model | explicit/template/hybrid semantics deterministic and schema-backed |
| `WU-02` | Add initial supported template catalog | at least three versioned/revision-bound templates with validation coverage |
| `WU-03` | Define evidence model and provenance for candidate discovery | invocation/manifest/dependency/packaging/workflow evidence can be represented without becoming responsibility authority |
| `WU-04` | Implement stronger candidate discovery | discovery relies less on naming heuristics and produces deterministic candidates with inspectable evidence provenance |
| `WU-05` | Implement migration planner and preview | current declarations + discovery evidence can produce a loss-preserving proposal; no write occurs before a complete preview |
| `WU-06` | Implement template-backed and materialized migration | both outputs preserve project-owned responsibility facts; explicit facts outrank discovery suggestions |
| `WU-07` | Update adoption and agent contracts | guidance explains template selection, migration, evidence provenance, override precedence, and no-path-inference rule |
| `WU-08` | Self-adopt/evaluate Responsibility Map v2 and improved candidate discovery in the PTSIP repository | current explicit map remains valid; discovery improves without silently changing declared ownership; optional migration can be demonstrated without semantic loss |
| `WU-09` | Final self-hosted regression and package verification | exact release-candidate SHA has successful self-hosted release verification |
| `WU-10` | Final Specification freeze and Tool release preparation | release contract passes; reviewed Tool and Specification notes are present before publication |

## 15. Sequencing

Recommended dependency flow:

```text
WU-00
  |
  +--> WU-01 --> WU-02 -----------+
  |                                |
  +--> WU-03 --> WU-04 -----------+--> WU-05 --> WU-06
                                   |
                                   +--> WU-07 --> WU-08 --> WU-09 --> WU-10
```

Responsibility Map schema/template work and candidate-discovery evidence work may proceed in parallel after the initial Specification baseline because they have distinct implementation responsibilities. Migration begins only after both sides are stable enough to consume together.

Tool `0.3.6` therefore becomes the consolidated compatibility and evidence-quality release. A separate Tool `0.4.0` candidate-discovery phase is no longer planned from the previous document; a future `0.4.0` version should be planned only if a new, independently consequential scope emerges after 0.3.6.