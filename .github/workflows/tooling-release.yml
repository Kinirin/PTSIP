name: tooling-release

on:
  release:
    types: [published]

permissions:
  contents: read

concurrency:
  group: tooling-release-${{ github.event.release.tag_name }}
  cancel-in-progress: false

jobs:
  build:
    if: startsWith(github.event.release.tag_name, 'tool-v')
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout published Tool tag with full history
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
          fetch-depth: 0
          fetch-tags: true

      - name: Prepare self-hosted Python 3.14
        shell: powershell
        run: |
          $launcher = Get-Command py -ErrorAction SilentlyContinue
          if (-not $launcher) {
            throw "The self-hosted Windows runner must provide the Python launcher 'py' on PATH."
          }

          $python = (& py -3.14 -c "import sys; print(sys.executable)").Trim()
          if ($LASTEXITCODE -ne 0 -or -not $python) {
            throw "Python 3.14 is not available through 'py -3.14' on this self-hosted runner."
          }

          & $python -c "import sys; assert sys.version_info[:2] == (3, 14), sys.version; print(sys.version)"
          if ($LASTEXITCODE -ne 0) {
            throw "The selected host interpreter is not Python 3.14."
          }

          if (Test-Path -LiteralPath ".venv") {
            Remove-Item -LiteralPath ".venv" -Recurse -Force
          }

          & $python -m venv .venv
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to create the isolated release-build virtual environment."
          }

          $venvRoot = (Resolve-Path -LiteralPath ".venv").Path
          $venvScripts = Join-Path $venvRoot "Scripts"
          $venvPython = Join-Path $venvScripts "python.exe"
          if (-not (Test-Path -LiteralPath $venvPython -PathType Leaf)) {
            throw "Release-build virtual environment did not produce .venv\\Scripts\\python.exe."
          }

          & $venvPython --version
          "VIRTUAL_ENV=$venvRoot" >> $env:GITHUB_ENV
          $venvScripts >> $env:GITHUB_PATH
          Write-Host "Using host Python 3.14 via isolated environment: $venvPython"

      - name: Install distribution tooling
        shell: powershell
        run: python -m pip install --upgrade pip build twine "PyYAML>=6.0,<7"

      - name: Verify release tag matches package version and Specification contract
        shell: powershell
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          $packageVersion = (python -c "import tomllib; f=open('pyproject.toml','rb'); print(tomllib.load(f)['project']['version']); f.close()").Trim()
          $expectedTag = "tool-v$packageVersion"
          if ($env:RELEASE_TAG -ne $expectedTag) {
            throw "Release tag $env:RELEASE_TAG does not match package version $packageVersion. Expected $expectedTag."
          }

          $headTag = (git tag --points-at HEAD | Where-Object { $_ -eq $env:RELEASE_TAG } | Select-Object -First 1)
          if (-not $headTag) {
            throw "Checked out commit is not pointed to by release tag $env:RELEASE_TAG."
          }

          python .github/scripts/verify_release_contract.py

      - name: Build distributions
        shell: powershell
        run: python -m build

      - name: Verify wheel and sdist exclude tests
        shell: powershell
        run: |
          @'
          from pathlib import Path, PurePosixPath
          import tarfile
          import zipfile

          wheel = next(Path("dist").glob("*.whl"))
          sdist = next(Path("dist").glob("*.tar.gz"))

          with zipfile.ZipFile(wheel) as archive:
              wheel_tests = [
                  name for name in archive.namelist()
                  if "tests" in PurePosixPath(name).parts
              ]
          if wheel_tests:
              raise SystemExit(f"wheel contains tests/: {wheel_tests}")

          with tarfile.open(sdist, "r:gz") as archive:
              sdist_tests = [
                  member.name for member in archive.getmembers()
                  if "tests" in PurePosixPath(member.name).parts
              ]
          if sdist_tests:
              raise SystemExit(f"sdist contains tests/: {sdist_tests}")
          '@ | python -

      - name: Verify distribution metadata
        shell: powershell
        run: |
          $distFiles = Get-ChildItem -Path dist -File | ForEach-Object { $_.FullName }
          if (-not $distFiles) {
            throw "No distributions were produced under dist/."
          }
          python -m twine check $distFiles

      - name: Upload verified distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

  publish:
    if: startsWith(github.event.release.tag_name, 'tool-v')
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download self-hosted verified distributions
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      # pypa/gh-action-pypi-publish is Docker-based and requires GNU/Linux.
      # Keep only this narrow Trusted Publishing boundary on GitHub-hosted Linux.
      - name: Publish to PyPI with Trusted Publishing
        uses: pypa/gh-action-pypi-publish@release/v1
