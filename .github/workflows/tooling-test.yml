name: tooling-test

on:
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: tooling-test-self-hosted
  cancel-in-progress: false

jobs:
  test:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Checkout exact dispatched source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          fetch-tags: true

      - name: Verify exact dispatched source identity
        id: source
        shell: powershell
        env:
          DISPATCHED_SHA: ${{ github.sha }}
        run: |
          $actualSha = (git rev-parse HEAD).Trim()
          if ($actualSha -ne $env:DISPATCHED_SHA) {
            throw "Checked out SHA $actualSha does not match workflow-dispatch SHA $env:DISPATCHED_SHA."
          }
          "verified_sha=$actualSha" >> $env:GITHUB_OUTPUT
          Write-Host "Verification source SHA: $actualSha"
          Write-Host "Self-hosted runner: $env:RUNNER_NAME"

      - name: Prepare self-hosted Python 3.14
        shell: powershell
        run: |
          $launcher = Get-Command py -ErrorAction SilentlyContinue
          if (-not $launcher) {
            throw "The self-hosted Windows runner must provide the Python launcher 'py' on PATH."
          }

          $python = (& py -3.14 -c "import sys; print(sys.executable)").Trim()
          if ($LASTEXITCODE -ne 0 -or -not $python) {
            throw "Python 3.14 is not available through 'py -3.14' on this self-hosted runner."
          }

          & $python -c "import sys; assert sys.version_info[:2] == (3, 14), sys.version; print(sys.version)"
          if ($LASTEXITCODE -ne 0) {
            throw "The selected host interpreter is not Python 3.14."
          }

          if (Test-Path -LiteralPath ".venv") {
            Remove-Item -LiteralPath ".venv" -Recurse -Force
          }

          & $python -m venv .venv
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to create the isolated verification virtual environment."
          }

          $venvRoot = (Resolve-Path -LiteralPath ".venv").Path
          $venvScripts = Join-Path $venvRoot "Scripts"
          $venvPython = Join-Path $venvScripts "python.exe"
          if (-not (Test-Path -LiteralPath $venvPython -PathType Leaf)) {
            throw "Verification virtual environment did not produce .venv\\Scripts\\python.exe."
          }

          & $venvPython --version
          "VIRTUAL_ENV=$venvRoot" >> $env:GITHUB_ENV
          $venvScripts >> $env:GITHUB_PATH
          Write-Host "Using host Python 3.14 via isolated environment: $venvPython"

      - name: Install verification tooling
        shell: powershell
        run: |
          python -m pip install --upgrade pip build twine
          python -m pip install -e ".[dev]"

      - name: Run complete repository regression
        shell: powershell
        run: python -m pytest -q

      - name: Verify Tool, Specification, and repository profile
        shell: powershell
        run: |
          ptsip --version
          ptsip spec
          ptsip validate . --json
          ptsip clarify . --json
          ptsip gate . --coordination local --json
          ptsip conform --help

      - name: Build and verify distributions
        shell: powershell
        run: |
          python -m build
          $distFiles = Get-ChildItem -Path dist -File | ForEach-Object { $_.FullName }
          if (-not $distFiles) {
            throw "No distributions were produced under dist/."
          }
          python -m twine check $distFiles

      - name: Reinstall built wheel and smoke public surfaces
        shell: powershell
        run: |
          $wheel = Get-ChildItem -Path dist -Filter *.whl | Select-Object -First 1
          if (-not $wheel) {
            throw "No wheel was produced under dist/."
          }
          python -m pip install --force-reinstall --no-deps "$($wheel.FullName)"
          ptsip --version
          ptsip spec
          ptsip conform --help
          python -c "from vpms import VerificationPurpose; assert VerificationPurpose.PRODUCT.value == 'PRODUCT'; assert VerificationPurpose.TOOLCHAIN.value == 'TOOLCHAIN'"

      - name: Record successful exact-SHA tooling verification
        shell: powershell
        env:
          GITHUB_TOKEN: ${{ github.token }}
          VERIFIED_SHA: ${{ steps.source.outputs.verified_sha }}
        run: |
          $headers = @{
            Authorization = "Bearer $env:GITHUB_TOKEN"
            Accept = "application/vnd.github+json"
            "X-GitHub-Api-Version" = "2022-11-28"
          }
          $body = @{
            state = "success"
            context = "self-hosted/tooling-test"
            description = "Self-hosted Windows verification passed for the exact dispatched SHA"
            target_url = "$env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions/runs/$env:GITHUB_RUN_ID"
          } | ConvertTo-Json

          Invoke-RestMethod `
            -Method Post `
            -Uri "$env:GITHUB_API_URL/repos/$env:GITHUB_REPOSITORY/statuses/$env:VERIFIED_SHA" `
            -Headers $headers `
            -ContentType "application/json" `
            -Body $body
