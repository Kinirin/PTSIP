# Support Policy Automation

This directory is reserved for repository-side automation that validates or projects the canonical Support Feature Policy under `../policy/`.

It is not `developer/automation/`, does not contain Developer Policy, and does not create or infer Support Policy semantics. The canonical policy authority is `docs/Support_policy/policy/`.
