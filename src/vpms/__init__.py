"""VPMS — Verification Purpose Management System.

The VPMS package is intentionally owned as a sibling of ``ptsip``.  Public
VPMS semantics are introduced incrementally by the Tool 0.3.5 work units;
this package root deliberately imports neither PTSIP core nor future VPMS
implementation modules so that the subsystem starts from an isolated
ownership boundary.
"""

from __future__ import annotations

__all__: tuple[str, ...] = ()
